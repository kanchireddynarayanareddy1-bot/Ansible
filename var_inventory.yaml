- name: variables from inventory
  hosts: local
  connection: local
  tasks:
  - name: print course info
    ansible.builtin.debug:
     msg: "The cousrse name is {{ course }}, course duration {{ duration}} and the trainer is {{ trainer}}"